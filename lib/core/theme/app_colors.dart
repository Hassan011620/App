import 'package:flutter/material.dart';

class AppColors {
  // ── Backgrounds ──────────────────────────────────────────
  static const Color deepBackground   = Color(0xFF0A0A0A);
  static const Color mediumBackground = Color(0xFF111111);
  static const Color surfaceBackground= Color(0xFF1A1A1A);
  static const Color elevatedSurface  = Color(0xFF222222);

  // ── Text ─────────────────────────────────────────────────
  static const Color textPrimary   = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFE5E5E5);
  static const Color textMuted     = Color(0xFFA3A3A3);
  static const Color textDisabled  = Color(0xFF555555);

  // ── States ───────────────────────────────────────────────
  static const Color success   = Color(0xFF22C55E);
  static const Color error     = Color(0xFFEF4444);
  static const Color pending   = Color(0xFFF59E0B);
  static const Color scheduled = Color(0xFF3B82F6);
  static const Color escrow    = Color(0xFF8B5CF6);
  static const Color info      = Color(0xFF06B6D4);

  // ── Glass ────────────────────────────────────────────────
  static const Color glassLight  = Color(0x08FFFFFF);
  static const Color glassMedium = Color(0x0DFFFFFF);
  static const Color glassHeavy  = Color(0x12FFFFFF);
  static const Color glassBorder = Color(0x14FFFFFF);

  // ── Borders ──────────────────────────────────────────────
  static const Color borderSubtle  = Color(0x0DFFFFFF);
  static const Color borderDefault = Color(0x14FFFFFF);
  static const Color borderStrong  = Color(0x26FFFFFF);
  static const Color borderGlow    = Color(0x40FFFFFF);
}
